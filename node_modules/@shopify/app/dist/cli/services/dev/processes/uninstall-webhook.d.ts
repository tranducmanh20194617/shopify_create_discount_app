import { BaseProcess, DevProcessFunction } from './types.js';
import { Web } from '../../../models/app/app.js';
import { DeveloperPlatformClient } from '../../../utilities/developer-platform-client.js';
export interface SendWebhookOptions {
    deliveryPort: number;
    developerPlatformClient: DeveloperPlatformClient;
    storeFqdn: string;
    apiSecret: string;
    webhooksPath: string;
}
export interface SendWebhookProcess extends BaseProcess<SendWebhookOptions> {
    type: 'send-webhook';
}
export declare const sendWebhook: DevProcessFunction<SendWebhookOptions>;
export declare function setupSendUninstallWebhookProcess({ webs, remoteAppUpdated, backendPort, frontendPort, ...options }: Pick<SendWebhookOptions, 'developerPlatformClient' | 'storeFqdn' | 'apiSecret'> & {
    remoteAppUpdated: boolean;
    backendPort: number;
    frontendPort: number;
    webs: Web[];
}): SendWebhookProcess | undefined;
