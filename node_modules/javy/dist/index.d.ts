interface JavyBuiltins {
    IO: {
        readSync(fd: number, buffer: Uint8Array): number;
        writeSync(fd: number, buffer: Uint8Array): number;
    };
}
declare global {
    const Javy: JavyBuiltins;
}
export declare const enum STDIO {
    Stdin = 0,
    Stdout = 1,
    Stderr = 2
}
export {};
