import type { ValidateFunction } from '../types';
export interface SchemaError {
    path: string;
    message: string;
}
/**
 * Wraps a json schema validator with formatted error messages for rendering
 */
export declare const withErrorFormatting: (validate: ValidateFunction) => (sectionSchema: object) => SchemaError[];
