import { Hook } from '@oclif/core';
export declare function findAlternativeCommand(opts: Pick<Parameters<Hook.CommandNotFound>[0], 'id' | 'argv' | 'config'>): string | undefined;
export declare function shouldRunCommand(result: string, userCommand: string): Promise<boolean>;
declare const hook: Hook.CommandNotFound;
export default hook;
