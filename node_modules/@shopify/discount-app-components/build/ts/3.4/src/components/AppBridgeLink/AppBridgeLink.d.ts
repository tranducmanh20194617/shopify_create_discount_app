import React from 'react';
import { LinkAction } from '../../types';
export interface AppBridgeLinkProps {
    children?: React.ReactNode;
    external?: boolean;
}
export declare const AppBridgeLink: ({ children, ...rest }: AppBridgeLinkProps & LinkAction) => React.JSX.Element;
