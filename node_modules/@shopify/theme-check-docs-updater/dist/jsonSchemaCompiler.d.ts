/**
 * Attempts to compile all the json schemas within a directory
 *
 * All compatible schemas will be output to the same directory
 */
export declare const compileJsonSchemaToFile: (dir: string) => Promise<void>;
export declare const compileJsonSchema: (jsonSchema: any) => import("ajv").ValidateFunction<unknown>;
