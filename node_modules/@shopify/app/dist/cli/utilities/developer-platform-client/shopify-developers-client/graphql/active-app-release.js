import { gql } from 'graphql-request';
export const ActiveAppReleaseQuery = gql `
  query activeAppRelease($appId: ID!) {
    app(id: $appId) {
      id
      activeRelease {
        id
        version {
          modules {
            gid
            uid
            handle
            config
            specification {
              identifier
              externalIdentifier
              name
              experience
            }
          }
        }
      }
    }
  }
`;
//# sourceMappingURL=active-app-release.js.map