import { gql } from 'graphql-request';
export const CreateAppVersionMutation = gql `
  mutation CreateAppVersion(
    $appId: ID!
    $appModules: [NewModuleVersion!]!
    $assetsUrl: String
    $versionTag: String
    $gitUrl: String
  ) {
    versionCreate(
      appId: $appId
      modules: $appModules
      assetsUrl: $assetsUrl
      versionTag: $versionTag
      gitUrl: $gitUrl
    ) {
      version {
        id
        modules {
          gid
          uid
          handle
          config
          specification {
            identifier
            name
            experience
          }
        }
        versionTag
      }
      userErrors {
        field
        message
      }
    }
  }
`;
//# sourceMappingURL=create-app-version.js.map